# Validation Checks

Put always-on validation checklist files here.

The harness loads `.md`, `.markdown`, and `.txt` files from this folder after a skill is selected. Codex receives these checks before generating the final Markdown output.

Use this folder for checks such as required sections, completeness criteria, quality gates, terminology checks, source traceability, or final review rules.
